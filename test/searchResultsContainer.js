/* global searchResultsContainer, describe, it, expect, should */

describe('searchResultsContainer()', function () {
  'use strict';

  it('exists', function () {
    expect(searchResultsContainer).to.be.a('function');

  });

  it('does something', function () {
    expect(true).to.equal(false);
  });

  it('does something else', function () {
    expect(true).to.equal(false);
  });

  // Add more assertions here
});
